version: 1
frontend:
  phases:
    preBuild:
      commands:
        - nvm install 20.19.0
        - nvm use 20.19.0
        - npm ci
    build:
      commands:
        - npm run build
  artifacts:
    baseDirectory: dist/bike-brag/browser
    files:
      - '**/*'
  cache:
    paths:
      - node_modules/**/*
